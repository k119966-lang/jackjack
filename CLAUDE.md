# CLAUDE.md — 슈리링의원 홈페이지

이 저장소에서 작업할 때 반드시 지킬 제약을 정리한 문서입니다.
상세 계획은 `PLAN.md`, 참고 자료 분석은 `reference/README.md`에 있습니다.

---

## 프로젝트

- **병원명** 슈리링의원 (한자 殊璃鈴 / 중국어 간체 殊璃铃)
- **위치** 서울 강남구 강남대로 438 스타플렉스(CGV 건물) 301호
- **규모** 11평, 원장 1인 운영, 직원 없음
- **진료** 슈링크(집속초음파) 중심 피부시술
- **운영** 30분 단위 완전 예약제 / 10:00–19:00 / 점심 13:00–14:00 / 일요일 휴진
- **브랜드** 방울(鈴). 절제된 한국적 정갈함. **무속·사찰 느낌이 나면 실패**
- **타깃** 강남역 인근 직장인 + 중화권 유입(샤오홍슈) 소량

---

## 1. 최우선 제약 — 의료광고법

**디자인이나 전환율보다 우선합니다.** 위반 시 실제 행정처분·형사처벌 대상입니다.

### 절대 넣지 않는 것

치료경험담 · 후기 · 리뷰 · 별점 · 환자 인터뷰 · 시술 전후 사진 · 비포애프터 ·
시술 장면 사진/영상 · 타 병원 비교 · 비방 · 할인/이벤트/선착순 · 비의료인·연예인 추천 ·
기사형 광고(`언론보도` 섹션) · 자체 작명 상품명 · 인증마크

### 금지 표현 (구현 후 HTML 전문에 정규식으로 검사)

```
없습니다 | 없어요 | 없는 | 무통 | 다운타임\s*(제로|0|없)
최고 | 최상 | 유일 | 국내\s*최초 | 1위 | 명의 | 전문\s*클리닉
특가 | 한정 | 이벤트 | 할인 | 선착순 | 혜택
즉시\s*효과 | 효과\s*보장 | 반영구 | 영구
```

`통증이 없습니다`는 **형사 유죄 사례가 있는 대표 위험 문구**입니다.
`없다`는 단정 대신 개인차를 명시하는 서술로 씁니다.

### 부작용 고지 — CSS 제약이 법에서 나옵니다

시행령 제23조가 중요정보를 *"빠뜨리거나 글씨 크기를 작게 하는 등의 방법으로
눈에 잘 띄지 않게"* 하는 것을 금지합니다. **배치와 가독성까지 심사 대상입니다.**

| 항목 | 요건 |
|---|---|
| 글자 크기 | 본문과 동일 (최소 15~16px). 줄이지 않음 |
| 색 대비 | 본문과 동일 (WCAG AA 4.5:1 이상). `--sand` 같은 흐린 색 금지 |
| 배치 | 시술 설명 블록 **바로 아래** |
| 접기 | **아코디언 기본 닫힘 금지** |
| 반복 | 비급여 가격표 섹션에 1회 더 노출 |

서술은 5요소 순서: ① 이상반응 항목 → ② 가역성 구분 → ③ 빈도 뉘앙스(`흔히`/`드물게`,
**근거 없는 구체 수치 금지**) → ④ 적합하지 않을 수 있는 대상 →
⑤ `시술 여부와 적합성은 진료 후 의사가 판단합니다`

### 명칭 표기 (의료법 제42조 · 시행규칙 제40조)

- 의료기관 명칭은 **`고유명칭 + 종류명칭`** 구조. **종류명칭은 고유명칭의 1/2 이상 크기**
- ⛔ 헤더·h1·로고에서 **`의원`만 작게 줄이지 않습니다**
- ⛔ `슈리링` 단독 사용 금지. `슈리링클리닉` 같은 **변형 표기 금지**
- `<title>` · `og:title` · `h1` · JSON-LD `name` 전부 **`슈리링의원`** 전체 명칭

### 전문과목 표기 — 확정됨

**원장은 피부과 전문의가 아니라 일반의(면허 종류: 의사)입니다.**
따라서 전문과목을 표시할 수 없습니다.
`피부과 전문의` `전문 클리닉` `전문의 직접 시술` 표현 금지.
JSON-LD 의 `medicalSpecialty` 도 넣지 않았습니다 —
검색 결과에 '피부과'로 노출되면 전문과목 표방 문제가 되기 때문입니다.

### 원장 약력

객관적 사실만 둡니다. 현재 두 줄입니다.
- 부산대학교 의과대학 졸업
- 대한 리프팅 학회

⛔ `풍부한 경험` `숙련된` `다수 시술` 같은 평가·수량 과시 표현 금지
⛔ 6개월 이하의 임상경력은 광고 금지 대상

**학회 표기 — 게재 전 확인 필요**
- 관계를 명시할 것. 학회명만 적으면 독자가 '자격'으로 오인할 수 있습니다.
  권장: `대한리프팅학회 정회원` 처럼 관계를 붙일 것
- 공식 명칭과 한 글자도 다르지 않게 적을 것 (통상 붙여 씀: 대한리프팅학회).
  사실과 다르면 제56조의 거짓 표시입니다
- ⛔ 학회가 주는 `인증의` 같은 명칭은 법적 자격이 아닙니다.
  자격처럼 적으면 `법적 근거 없는 자격 표방`, `인증·보증·추천을 받았다` 는
  취지로 읽히면 그 자체로 금지 대상입니다

### ⚠️ 원장 소개 문구 — 알려진 위험

원장 요청으로 아래 문구를 게재했습니다. **교체가 쉽도록 블록을 분리해 두었습니다.**

> 23곳의 의원을 돌아온 끝에 도달한 하나의 리프팅

치료경험담(제2호) · 비교(제4호) · 유일성 암시(제8호) · 효과 오인(제2호)이 겹칩니다.
`tools/check-copy.py` 가 이 문구로 4건을 검출하는 것은 **정상 동작**입니다.
대안 문안은 `README.md` 의 '카피 대안' 절에 있습니다. 임의로 지우지 말고 원장과 상의하십시오.

### 비급여 가격 — 확정된 조건

| 항목 | 진료비용(부가가치세 포함) |
|---|---|
| 1샷 | 1,650원 (1,500원 + 부가세) |
| 1,000샷 이상 구성 | **1,500,000원 (부가세 포함)** |

⚠️ **1,000샷 구성은 사실상 수량 할인입니다.**
1샷 단가 기준 1,000샷 = 1,650,000원이므로 150,000원(약 9%) 낮습니다.
비급여 할인 자체는 금지되지 않지만, **할인으로 소구하면 제56조 규제 대상**이 됩니다.
- ⛔ `할인` `혜택` `특가` `이벤트` 단어 사용 금지
- ⛔ 1샷 단가와 비교해 얼마를 아낀다는 소구 문구 금지
- ✅ 두 항목을 각각 정확한 금액으로 나열하는 '고지'에 머무를 것

### 비급여 가격표 — "광고"가 아니라 "고지"

제45조 고지 의무와 제56조 광고 규제가 충돌하는 지점입니다.
**이 섹션만은 배경색·장식·카피 톤 없이 표만 둡니다.** 방울 파문 그래픽도 쓰지 않습니다.
권유형 문장 자체를 넣지 않습니다.

### 장비 서술

- 식약처 **허가 사용목적 범위를 넘으면 의료기기법 제24조 위반**
- 성능·효능·효과는 **허가 문구를 그대로 인용**하는 방식으로만
- ⛔ 제조사가 받은 의료기기 광고 심의번호를 의원 홈페이지에 옮겨 적지 않음
- 기전 서술은 **"열응고점을 만든다"까지만**. 콜라겐 재생·탄력 회복 등 이후 단계는 효과 주장

### 근거 표기

홈페이지에 근거를 적을 때 **`의료법 제56조`까지만 쓰고 항·호는 인용하지 않습니다.**
2018.9.28. 개정 전후로 항 번호가 달라 잘못 적을 위험이 있습니다.

---

## 2. 개인정보 (예약 폼)

수집 항목은 **이름 · 연락처만**. 보유기간 **수집일로부터 1년**.

**제15조제2항 4대 고지 사항** — 하나라도 빠지면 과태료
1. 수집·이용 목적  2. 수집 항목  3. 보유·이용 기간
4. **동의 거부 권리 및 거부에 따른 불이익** ← 법정 필수

| 항목 | 요건 |
|---|---|
| 사전 체크 | ⛔ `checked` 기본값 금지 |
| 전체 동의 | ⛔ `전체 동의` 하나만 두고 개별 동의 생략 금지 (제22조제1항 위반) |
| 보유기간 | `1년`만 쓰지 말고 **기산점 명시** (`수집일로부터 1년`) |
| 주민등록번호 | ⛔ 수집 불가 |
| 전송 | HTTPS 필수 |

⛔ **`시술 항목` `관심 부위` `증상` 필드 금지** — 민감정보(건강정보)가 되어 별도 동의 필요

개인정보 처리방침은 **1인 의원도 의무**(제30조, 규모 예외 없음). `privacy.html`로 분리.
1인 의원은 보호책임자를 별도 지정하지 않아도 되나 **그 경우 원장이 자동으로 보호책임자**.

---

## 3. 디자인 토큰 (고정값, 변경 금지)

```css
--ink:       #1A1714;  /* 먹색   본문·헤더 */
--paper:     #F4EFE6;  /* 종이색 배경 */
--bronze:    #A8895A;  /* 브론즈골드 */
--bronze-hi: #C9A96A;  /* 하이라이트 */
--seal:      #9E3B2F;  /* 인주색 포인트에만 아주 소량 */
--line:      #D9D1C1;  /* 구분선 */
--sand:      #8D8272;  /* 보조 */
--sand-text: #736A5C;  /* 추가 — paper 위 보조 텍스트 전용 (4.65:1) */
```

### 명암비 실측값 — 용도 제한

| 토큰 | paper 위 | ink 위 | 허용 용도 |
|---|---|---|---|
| `--ink` | 15.58:1 | — | 제한 없음 |
| `--seal` | 5.88:1 ✅ | 2.65:1 ❌ | paper 위 포인트만. **ink 배경 위 금지** |
| `--sand` | 3.29:1 ⚠️ | 4.73:1 ✅ | **본문 텍스트 불가.** 입력 테두리·아이콘 경계·ink 위 텍스트 |
| `--sand-text` | 4.65:1 ✅ | — | paper 위 보조 텍스트 |
| `--bronze` | 2.87:1 ❌ | 5.43:1 ✅ | paper 위 **텍스트 금지**. 장식·파문. ink 위 텍스트 가능 |
| `--bronze-hi` | 1.96:1 ❌ | 7.96:1 ✅ | 동일 |
| `--line` | 1.32:1 | 11.76:1 | **장식용 구분선 전용.** 입력 테두리로 쓰면 안 됨 |

> 로고 실측: 글자 `#1A1714`(= `--ink` 완전 일치), 배경 `#F5F1E8`(≈ `--paper`),
> 匠 인장 `#D6392C`(≠ `--seal`). 인장 색 불일치는 의도적으로 두고 토큰을 우선합니다.

### 로고

- `assets/logo.svg` — 전체 락업 / `assets/logo-mark.svg` — 匠 인장만(헤더·파비콘)
- `reference/01-logo.png` 를 `tools/trace-logo.py` 로 벡터화한 것입니다
- 디자이너의 원본 벡터 파일이 있으면 그것으로 교체하십시오
- ⛔ 헤더에서 `의원`만 작게 줄이지 마십시오 (시행규칙 제40조)

### 방울(鈴) 파문

- **동심원만.** 인라인 SVG, 원 3~4개, `stroke`는 `--line`/`--bronze`, `fill` 없음, 낮은 불투명도
- 히어로 1곳 + 섹션 전환 1~2곳으로 **제한**. 반복하면 패턴이 되어 정갈함이 깨짐
- ⛔ 종·풍경 아이콘, 연꽃, 단청 배색, 태극, 부적 문양, 붓글씨 질감
- ⛔ 비급여 가격표 섹션에는 쓰지 않음

### 폰트

- 제목 **Noto Serif KR** 300/400
- 본문 **Noto Sans KR** 300/400/500
- 영문·숫자 악센트 **Cormorant Garamond** italic 300/400
- 본문 `line-height: 1.72` 이상 / `word-break: keep-all` / `letter-spacing: -0.01em`
  (작은 글자 11~14px에는 음수 자간 적용하지 않음)

---

## 4. 기술 요건

- HTML + CSS + vanilla JS. **프레임워크·빌드 도구 없음**
- **외부 요청은 Google Fonts만** (지도 임베드를 넣지 않는 이유)
- 모바일 우선. **375px에서 먼저 완성 후 데스크톱 확장.** 본문 최대 폭 880px
- 시맨틱 태그, 이미지 alt, 명암비 4.5:1 이상, 키보드 포커스 표시
- `prefers-reduced-motion` 대응, 애니메이션 최소
- 이미지는 넣지 말고 **자리만 비우고 주석으로 권장 규격 표기**
- 코드에 **한글 주석**으로 섹션 구분

---

## 5. 참고 자료 사용 규칙

`reference/`는 **PEGASUS CLINIC**(일본 미용클리닉) 스크린샷입니다.

- ✅ 가져와도 됨 — 섹션 순서·정보 구조, 내비 방식, 여백 리듬, 타이포 위계, 인터랙션 패턴
- ⛔ 절대 금지 — 문구·카피 한 문장도, 이미지·아이콘·로고·폰트, CSS 코드·클래스명·변수명, 브랜드 색상

`reference/11-before-after-FORBIDDEN-IN-KR.png`는 **한국에서 금지된 구성**입니다. 채용 금지.

---

## 6. 작업 규칙

- 브리핑에 없는 사실은 **절대 지어내지 말고 플레이스홀더**로 남깁니다
- 플레이스홀더는 `{{이중중괄호}}`, 목록은 `index.html` 최상단 `<!-- TODO -->` 주석에 모읍니다
- 시술 설명·부작용 문안은 **원장 감수 필수**. 개발자가 임의로 확정하지 않습니다
- 커밋은 섹션 단위로
