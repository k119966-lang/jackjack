# 슈리링의원 홈페이지 — 1단계 계획서

> 이 문서는 **승인 요청용**입니다. 승인 전까지 `index.html` / `css/` / `js/` 는 작성하지 않습니다.
> 현재까지 만든 것은 `reference/`(참고 자료 정리)와 이 문서뿐입니다.

---

## 0. 전제와 확인한 사실

| 항목 | 상태 |
|---|---|
| 참고 사이트 URL | **전달받지 못함.** 브리핑의 `{{참고사이트 URL}}`이 플레이스홀더 그대로였습니다. |
| 참고 스크린샷 | **확보.** `origin/main`에 올라온 29장 중 중복 13장을 제외한 16장. `reference/`에 정리 |
| 참고 사이트 정체 | 스크린샷으로 식별: **PEGASUS CLINIC** (일본 미용클리닉) |
| 로고 | `reference/01-logo.png` (177×232 캡처). **원본 파일 대기 중** |

URL 접근은 시도할 대상 자체가 없었으므로 **스크린샷만으로 분석**했습니다.
따라서 실제 CSS·스크롤 동작·호버·트랜지션은 확인할 수 없었고, 정지 이미지에서
관찰 가능한 범위로만 판단했습니다.

### 참고 자료의 결정적 공백 — 모바일 캡처가 없습니다

스크린샷 15장이 **전부 데스크톱(폭 1525~1850px)** 입니다.
브리핑 §0은 "모바일에서의 CTA 배치"를 참고 대상으로 지정했지만,
이 자료에서는 관찰할 수 없습니다. **모바일 하단 고정 바는 참고 없이 새로 설계합니다.**
(모바일 캡처를 주시면 그 부분만 다시 맞추겠습니다.)

또한 캡처마다 뷰포트 폭이 달라 **절대 px값은 이식 불가**이고 비율만 유효합니다.

---

## 1. 참고 사이트에서 가져올 구조적 요소

### 가져오는 것

| # | 요소 | 관찰 위치 | 우리 적용 |
|---|---|---|---|
| 1 | **섹션 헤더 3단 패턴** — 영문 소문자 라벨(자간 넓음) → 짧은 괘선 → 국문 대제목 → 리드문 | `06-reason-2col` | 전 섹션 공통 패턴으로 채용. 가장 강하게 가져오는 요소 |
| 2 | **리드문 좌측 세로 괘선** (본문 왼쪽에 1px 세로선 + 들여쓰기) | `06`, `08` | 각 섹션 리드문에 적용 |
| 3 | **2단 교차 레이아웃** — 텍스트/이미지를 행마다 좌우 교차 | `08-treatment-items` | 진료 안내의 시술 단계에 적용 (데스크톱만, 모바일은 1단) |
| 4 | **번호 + 얇은 밑줄 헤딩** (`01` `02` 형태) | `08`, `09` | 시술 과정 단계 번호에 적용 |
| 5 | **고스트 워터마크 대형 영문** (배경에 옅은 큰 글자) | `05`, `06`, `10` | **아주 절제해서** 1~2곳만. 방울 파문과 충돌하지 않는 선에서 |
| 6 | **완전예약제 고지를 예약 진입 바로 위에** 배치 | `14-contact-split` | 그대로 채용. 우리도 완전예약제라 동일하게 유효 |
| 7 | **대형 전화번호 조판** (숫자를 세리프로 크게) | `14` | 채용. 브리핑이 지정한 Cormorant Garamond를 숫자 악센트로 사용 |
| 8 | **휴진일 캘린더** | `15-calendar` | **발상만 차용**, 구현은 보류 (아래 3-7 참조) |
| 9 | **시술 리스크를 표의 한 행으로 명시** | `11` | 표현은 버리고 **"위험을 표로 명시한다"는 구조만** 차용 |
| 10 | **필(pill) 형태 아웃라인 버튼** | `07`, `08` | 형태만 차용, 색·테두리는 우리 토큰으로 |
| 11 | **본문 행간 리듬** (실측 약 1.69) | `06` | 브리핑 요구치 1.72와 사실상 동일 → 검증됨 |
| 12 | **타이포 위계 비율** (대제목 ≈ 본문 1.9× / 소제목 1.25× / 리드 1.07×) | `06` | 우리 스케일 설계의 기준선으로 사용 |

### 의도적으로 버리는 것 — 그리고 이유

| 요소 | 참고 사이트 | 버리는 이유 |
|---|---|---|
| **증례사진(비포·애프터)** | `11` | ⛔ **한국 의료법상 금지.** 브리핑 §2 명시. 타협 없음 |
| **후기 팝업 토스트** (좌하단에 환자 후기가 계속 뜸) | 거의 전 캡처 | ⛔ 치료경험담. 금지 |
| **원장 경력 체크리스트** ("7년간 원장", "시술 경험 풍부") | `05` | 경력·역량 과시로 읽혀 심의 위험. 원장 정보는 **성명·면허 사실**만 |
| **증상 해시태그 필** (`#눈밑지방` 등) | `08`, `09` | 증상 유인형 표현. 단일 시술 병원에 불필요 |
| **고민별 아이콘 그리드** (5열 × 3행) | `10` | 우리는 슈링크 단일 진료. 항목을 늘릴 이유가 없음 |
| **칼럼/블로그** | `12` | 원장 1인·직원 없음. 운영 부담이 실제로 감당 불가 |
| **NEWS 섹션** | `03` | 같은 이유. 갱신되지 않는 NEWS는 오히려 마이너스 |
| **3분할 패널 내비** | `13` | 단일 페이지라 앵커 링크로 충분 |
| **우측 세로 고정 CTA 레일** (LINE/WEB/진료일시) | 전 캡처 | 데스크톱 화면을 잠식. 우리는 헤더 + 모바일 하단 바로 충분 |
| **검색창 / SNS 아이콘** | `02` | 단일 페이지에 검색은 불필요. SNS 계정 정보 없음 |
| **다단 사이트맵 푸터** | `16` | 링크할 하위 페이지가 없음 |
| **풀블리드 대형 사진 다수** | `02`, `04` | 11평 규모. 큰 공간 사진은 실제와 어긋날 위험 |
| **브랜드 색상 (연보라/블루)** | 전 캡처 | 브리핑 §4 토큰 사용 |

---

## 2. 섹션 순서 — 두 곳 조정을 제안합니다

```
①  헤더
②  히어로
③  진료 안내                      슈링크 원리 · 시술 과정
⑥→④ 시술 전 확인사항 · 부작용 및 주의사항      ← 크게 앞으로
⑤→⑤ 비급여 진료비용 고지표
④→⑥ 진료 방식                    30분 단위 예약제 · 원장 1인 진료   ← 뒤로
⑦  진료시간 · 휴진 안내
⑧  오시는 길
⑨  예약
⑩  푸터
```

두 블록으로 나뉩니다.
**정보 블록(③④⑤)** — 무엇을 하는가 → 어떤 위험이 있는가 → 얼마인가
**실행 블록(⑥⑦⑧⑨)** — 어떻게 운영되는가 → 언제 → 어디서 → 예약

### 조정 1 — 부작용을 진료 안내 바로 뒤로 (가장 중요)

**법적 근거가 명확합니다.** 조사에서 확인된 내용:

> 의료법 시행령 제23조는 제56조제2항제7호(중요정보 누락)를
> **"부작용 등 중요 정보를 빠뜨리거나 글씨 크기를 작게 하는 등의 방법으로
> 눈에 잘 띄지 않게 광고하는 것"** 으로 구체화합니다.

즉 **기재 여부만이 아니라 배치와 가독성까지 심사 대상**입니다.
시술 설명과 위험 고지 사이에 가격표가 끼면 "멀리 떼어놓았다"는 지적을 받을 여지가 생깁니다.
조사의 권고도 **"시술 설명 블록 바로 아래"** 입니다.

> 브리핑 §2가 "부작용·주의사항 섹션을 반드시 **별도로** 둘 것"이라고 했으므로
> **독립 `<section>` + 자체 제목**은 유지하되, 진료 안내와 시각적으로 이어지도록
> 배경 띠를 공유하고 사이 여백을 줄여 **하나의 정보 단위로 읽히게** 하겠습니다.

### 조정 2 — 진료 방식을 예약 그룹 앞으로

`30분 단위 완전예약제 · 원장 1인 진료`는 **시술 설명이 아니라 운영 안내**입니다.
예약 직전(⑦진료시간 → ⑧오시는 길 → ⑨예약) 그룹의 머리에 두면
"완전예약제구나 → 그럼 시간은 → 위치는 → 예약" 으로 자연스럽게 이어집니다.
브리핑 위치(③ 뒤)에 두면 시술 설명과 부작용 사이를 갈라놓게 됩니다.

### 따르지 않은 참고 사이트 패턴

참고 사이트는 예약 진입 **뒤에** 캘린더를 둡니다(`15-calendar`).
예약을 누르고 난 다음에 휴진일을 보여주는 셈이라 순서가 뒤집힌 것으로 판단해 따르지 않습니다.

> 브리핑 순서 그대로가 낫다고 보시면 되돌리겠습니다. 구현 난이도 차이는 없습니다.
> 다만 **부작용을 가격표 뒤에 두는 것만은 법적으로 권하지 않습니다.**

---

## 3. 섹션별 레이아웃 방향

공통 규칙: **모바일 375px에서 먼저 완성 → 880px에서 확장.** 본문 최대 폭 880px.
좌우 여백 모바일 20px / 데스크톱 32px. 섹션 상하 여백 모바일 64px / 데스크톱 112px.

### ① 헤더

- **375px** — 높이 56px. 좌측 로고만. 전화·예약은 하단 고정 바가 담당하므로 헤더에 중복 배치하지 않음.
  햄버거 메뉴 **없음** (단일 페이지 · JS 최소화). 스크롤 시 그림자 없이 배경만 `--paper`로 고정.
- **≥880px** — 좌측 로고 / 중앙 앵커 내비 6개(진료 안내 · 진료 방식 · 주의사항 · 진료비용 · 진료시간 · 오시는 길) /
  우측 전화번호 + 예약 버튼.
- 최상단에 **skip link** (`본문 바로가기`), 포커스 시에만 노출.
- 로고: `<img>` 자리 + 텍스트 대체. 원본 파일이 올 때까지 **텍스트 조판**(殊璃鈴 + 슈리링의원)으로 표시.

### ② 히어로

- **375px** — 1단. 방울 파문 그래픽(인라인 SVG, 동심원)을 배경에 크게 깔고 우측 하단으로 흘려 잘라냄.
  병원명(한자 + 한글) → 한 줄 소개 → 예약 CTA + 전화 보조 링크.
- **≥880px** — 동일 구성에 좌측 정렬 유지, 파문을 더 크게. 사진 없음.
- **이미지를 쓰지 않습니다.** 브리핑 §5가 "이미지는 자리만" 이라고 했고,
  11평 규모에서 참고 사이트 같은 대형 공간 사진은 실제와 어긋납니다. 파문 그래픽이 그 자리를 대신합니다.
- 한 줄 소개 문구는 **효과·우수성 표현 없이** 진료 범위와 운영 방식만 사실로 기술.

### ③ 진료 안내 — 슈링크 원리와 시술 과정

- 섹션 헤더 3단 패턴 적용.
- **원리** — 작용 기전을 사실로만 서술. 근거 조사에서 확인된 내용(문헌 확인도 높음):
  - 초음파를 피부 속 특정 깊이에 집속해 그 지점의 온도를 **약 60~70℃**까지 올려
    **열응고점(TCP, Thermal Coagulation Point)** 을 형성합니다.
  - ⚠️ **서술은 "열응고점을 만든다"까지만.** 그 이후 단계(콜라겐 재생·리모델링·탄력 회복)는
    **효과 주장으로 읽히므로 쓰지 않습니다.**
- **깊이–조직층 대응표** (3행 소형 표. 문헌 확인도 높고 효과 주장이 아니라 안전):

| 깊이 | 대응 조직층 |
|---|---|
| 1.5mm | 표재 진피 |
| 3.0mm | 심부 진피 |
| 4.5mm | SMAS층(표재성 근건막계) · 목의 활경근 |

- **시술 과정** — `01 … 05` 번호 + 얇은 밑줄 헤딩. 375px 1단 세로 나열,
  ≥880px에서 좌우 교차 2단(참고 사이트 `08` 구조).
  ⚠️ 조사 결과 **시술 과정 순서·소요시간에는 신뢰할 만한 1차 출처가 없습니다.**
  검색된 자료는 전부 타 의원 마케팅 글이고 수치도 서로 어긋납니다.
  **원장이 실제로 하는 순서와 시간만** 적고, 시간은 `약 ○○분`이 아니라 **`○○~○○분` 범위**로 씁니다.
- **샷(shot) 정의를 반드시 명시합니다.** 샷당 과금이므로 정의가 없으면 분쟁 소지가 됩니다.
  조사 결과 '샷'은 **규제 용어가 아니라 업계 관용어**이고, 점(dot) 방식과 선(line) 방식에서
  산정 기준이 달라질 수 있습니다. → 아래 6-⑧ 결정 필요.
- ⚠️ **기기명을 특정해야 합니다.** `슈링크`(수출명 Ultraformer III)와
  `슈링크 유니버스`(수출명 Ultraformer MPT)는 **서로 다른 제품**입니다(업그레이드 관계).
  보유하지 않은 모델의 명칭·카트리지명을 쓰면 허위·과장 광고 소지가 됩니다. → 6-⑦
- ⚠️ **성능·효능·효과 서술은 식약처 허가 사용목적 문구를 그대로 인용하는 방식으로만** 합니다.
  허가사항을 벗어난 표시는 **의료기기법 제24조** 위반 소지입니다.
  코드에서는 이 문구를 별도 상수 블록으로 분리해 수정 이력을 추적하기 쉽게 하겠습니다.
- ⚠️ **제조사가 받은 의료기기 광고 심의번호를 의원 홈페이지에 옮겨 적지 않습니다.**
  (제조사 공식 사이트에 심의확인번호가 있지만 그건 제조사 것입니다.)
- 이미지 자리는 각 단계 옆에 주석으로 규격만 남기고 비움.
- ⚠️ 최종 문안은 **원장 감수 필수**입니다.

### ④ 시술 전 확인사항 · 부작용 및 주의사항  *(순서 조정: 진료 안내 바로 뒤로)*

- **브리핑이 "반드시 별도로 둘 것"이라고 못박은 섹션.** 독립 `<section>` + 자체 제목으로 둡니다.
- 다만 진료 안내와 **시각적으로 이어지게** 합니다(배경 띠 공유, 사이 여백 축소).
  법적으로 "시술 설명 바로 아래"가 요구되기 때문입니다.

#### ⚠️ 이 섹션은 CSS 제약이 법에서 나옵니다

시행령 제23조가 **"글씨 크기를 작게 하는 등의 방법으로 눈에 잘 띄지 않게"** 하는 것을
금지하므로, 아래는 디자인 선택이 아니라 **준수 사항**입니다.

| 항목 | 요건 |
|---|---|
| 글자 크기 | **본문과 동일** (최소 15~16px). 작게 줄이지 않음 |
| 색 대비 | 본문과 동일한 대비 유지 (**WCAG AA 4.5:1 이상**) — `--sand` 같은 흐린 색 금지 |
| 배치 | 시술 설명 블록 **바로 아래** |
| 접기 | **아코디언으로 접어 기본 닫힘 상태로 두지 않음** |
| 반복 | **비급여 가격표 섹션에도 1회 더 노출** |

> 푸터에 회색 10px 미세문자로 넣는 흔한 처리는 **그 자체로 위반**입니다.

#### 서술은 5요소 순서를 지킵니다

조사에서 정리된 "중요정보 누락을 피하는 실무 하한선"입니다.

1. 발생 가능한 이상반응 **항목 나열**
2. **가역성 구분** — 대부분 일시적 / 드물게 지속
3. **빈도 뉘앙스** — `흔히` / `드물게` 수준까지만. ⚠️ **근거 없는 구체 수치(%)는 쓰지 않습니다**
4. 시술이 **적합하지 않을 수 있는 대상**
5. **상담 안내** — `시술 여부와 적합성은 진료 후 의사가 판단합니다`로 마감
- 세 블록으로 구성합니다. 조사에서 확인된 내용을 반영했습니다.

  **1. 상담 시 반드시 알려주셔야 하는 사항** (체크 목록)
  임신·수유 가능성 / 심박동기·제세동기 등 이식형 전자기기 / 시술 부위 금속 매식물·보형물 /
  시술 부위 피부 감염·염증·상처 / 켈로이드 경향 / 항응고·항혈소판제 복용 /
  자가면역질환 / 최근 시술 이력
  > ⚠️ 조사 결과 이 목록의 상당 부분이 **임상 웹페이지 기반이라 "금기"로 단정할 근거가 부족**합니다.
  > 그래서 `금기` 대신 **`알려주셔야 하는 사항`** 으로 구성하고,
  > **"최종 가부는 진료 상담에서 결정합니다"** 문장을 반드시 붙입니다.
  > 확정 금기 목록은 **장비 첨부문서 원문**을 기준으로 다시 써야 합니다(6-⑦).

  **2. 시술 후 나타날 수 있는 반응 — 흔하고 일시적** (문헌 확인도 높음)
  홍반 · 부종 · 불편감 · 저릿함 · 압통

  **3. 드물지만 중대한 이상반응** (문헌 확인도 높음)
  열화상 · 신경 손상 및 자극 · 지방 위축 · 과색소침착
  > ⚠️ **이 블록을 생략하면 "중요 정보 누락"과 설명의무 위반 소지가 생깁니다.** 반드시 게재합니다.
  > 문헌은 "대다수 합병증은 경미·일시적이나 심한 열화상 또는 신경 손상의 위험은 존재한다"고 명시합니다.

- ⚠️ **`멍`, `근육통 유사 통증`은 이번 조사에서 1차 문헌 근거를 찾지 못했습니다.**
  국내 클리닉 자료에는 흔히 나오지만 근거가 확인되지 않아,
  게재한다면 `드물게 ~할 수 있습니다` 형태의 가능성 서술로만 씁니다.
- ⚠️ **`~는 없습니다` 형태의 부정형 안심 문구를 절대 쓰지 않습니다.**
  (`붓기가 없다`, `통증이 없다` 등) 문헌과 배치되고 분쟁 시 불리합니다.
- ⚠️ 축소·완화 표현(`대부분 금방 사라집니다`, `드물게 있지만 걱정 없습니다`)도 쓰지 않습니다. 현상만 기술합니다.
- ⚠️ **홈페이지 문구와 시술 동의서 문구를 일치시켜야 합니다.**
- 최종 문안은 **원장 감수 필수**입니다.

### ⑤ 비급여 진료비용 고지표

- `<table>` + `<caption>` + `<th scope>` 로 시맨틱하게. 375px에서 가로 스크롤 없이 읽히도록 설계.
#### ⚠️ 이 섹션은 "광고"가 아니라 "고지"입니다 — 톤을 의도적으로 무미건조하게

조사에서 확인된 핵심: 제45조 고지는 **정보 제공 의무**이고,
같은 가격을 강조·비교·할인 프레임으로 내세우는 순간
제56조제2항제13호(비급여 진료비용에 관해 소비자를 잘못 알게 할 우려)와
제8호(과장) 영역으로 넘어갑니다.

→ **이 섹션만은 배경색·장식·카피 톤을 넣지 않고 표만 둡니다.**
방울 파문 그래픽도 이 섹션에는 쓰지 않습니다.
`한정` `특가` `최저` `이벤트` `혜택` 류 수식어는 물론, **권유형 문장 자체를 넣지 않습니다.**

#### 열 구성 — VAT 이중 표기를 제안합니다

| 항목 | 단가(부가세 별도) | 실제 결제 금액 | 비고 |
|---|---|---|---|
| 슈링크(집속초음파) 1샷 | 1,500원 | **1,650원** | |
| 슈링크(집속초음파) 1,000샷 이상 구성 | 1,500,000원 | **1,650,000원** | |

표 아래 고정 문구:
> 표시 금액은 부가가치세를 포함한 실제 환자 부담 금액 기준이며, 고지 금액을 초과하여 징수하지 않습니다.
> 1,000샷 구성의 부위별 배분은 피부 상태와 시술 목표에 따라 상담에서 결정합니다.
> 시술 당일 최종 샷수와 총액을 확정해 동의를 받은 뒤 진행합니다.
> 최종 변경일: {{가격최종변경일}}

- **`최종 변경일` 표기를 넣습니다.** 가격 변경 시 갱신하는 것을 운영 절차로 고정하기 위해서입니다.
- ⚠️ 「비급여 진료비용 등의 고지 지침」이 **고시 항목 코드·분류명 기재**를 요구합니다.
  해당 코드를 확인해 열에 추가할지 결정이 필요합니다(6-⑨).
- ⚠️ **부작용 고지를 이 섹션에도 1회 반복 노출**합니다(위 ④ 참조). 짧은 요약 + ④로 가는 앵커 링크.

- 숫자는 Cormorant Garamond로 악센트.
- ⚠️ **"1,000샷 이상 구성"은 1,500원 × 1,000샷과 정확히 같은 금액**이므로 할인이 아닙니다.
  할인·패키지처럼 읽히지 않게 조판하고, **"이벤트/특가/한정" 류 표현을 일절 쓰지 않습니다.**
- ⚠️ 자체 작명 상품명을 쓰지 않습니다. 브리핑 지시대로 **설명형**으로만 표기합니다.

**조사에서 새로 확인된 사항**

- **홈페이지 게시는 의무입니다.** 의료법 제45조(비급여 진료비용 고지, 고지 금액 초과 징수 금지)와
  시행규칙 제42조의2에 따라, 홈페이지를 운영하는 의료기관은 오프라인 고지 **외에**
  홈페이지에 따로 표시해야 합니다.
  → 팝업·배너가 아니라 **상시 접근 가능한 위치**에 두고, **내비게이션에 링크를 포함**합니다.
  (데스크톱 앵커 내비에 `진료비용`을 넣기로 한 설계가 여기에 부합합니다.)
- 보건복지부 고지 지침과 **HIRA(건강보험심사평가원) 표준 서식 가이드**가 있습니다.
  열 구성을 이 표준에 맞추는 것이 안전합니다. → 구현 전 서식 확인 필요(6-⑨).
- ⚠️ **부위별 샷 배분표를 고정 게재하지 않습니다.** 근거 있는 표준 배분표가 존재하지 않고,
  게재하면 표시 금액과 실제 시술이 어긋나 **제45조의 "고지 금액 초과 징수" 분쟁**으로 갈 수 있습니다.
  → `1,000샷 구성의 부위별 배분은 피부 상태와 시술 목표에 따라 상담에서 결정합니다`로 처리.
- ⚠️ 표 아래에 **`시술 당일 최종 샷수와 총액을 확정해 동의를 받은 뒤 진행합니다`** 를 명시합니다.
- ❓ **부가세 포함 금액 병기 여부는 결정이 필요합니다** (아래 6-③).
  조사는 **병기를 권합니다.** 근거: 제45조제2항이 "고지·게시한 금액을 초과하여 징수할 수 없다"고 하는데,
  1,500원으로 게시하고 1,650원을 받으면 **"고지 금액 초과 징수"로 해석될 여지**가 있습니다.
  다만 **집속초음파 리프팅이 부가가치세법 시행령 제35조의 과세 열거 항목 중 어디에 해당하는지는
  유권해석 영역**이라 세무사 확인이 필요하고,
  조사는 **강남구보건소 의약과 유선 확인**도 권고합니다.

### ⑥ 진료 방식 — 30분 단위 예약제 · 원장 1인 진료  *(순서 조정: 예약 그룹 앞으로)*

- 2단 구성(≥880px) / 1단(375px).
- **30분 단위 완전 예약제**가 무엇을 뜻하는지 사실로 서술: 예약 없이 방문 시 진료 불가,
  한 타임에 한 명, 상담부터 시술·설명까지 원장이 연속으로 담당.
- ⚠️ 여기서 **"충분한 상담", "꼼꼼한", "세심한" 같은 평가형 수식어를 쓰지 않습니다.**
  운영 구조의 사실 기술로만 씁니다. (참고 사이트는 이 자리를 `Reason — 선택받는 이유`로 쓰는데,
  그건 우위 주장이라 우리는 채용하지 않습니다.)
- 직원이 없다는 사실에서 따라오는 안내(진료 중 전화 연결이 어려울 수 있음)를 이 섹션에 사실로 배치.

### ⑦ 진료시간 · 휴진 안내

- 요일 × 시간 표. 점심시간을 별도 행이 아니라 **시간 문자열 안에** 표기
  (`10:00–13:00 / 14:00–19:00`)해서 한 눈에 들어오게.
- 일요일 휴진을 표 안과 표 아래 주석 양쪽에 명시.
- 30분 단위 예약제이므로 **마지막 예약 가능 시각(18:30)** 을 함께 안내.
**조사에서 새로 확인된 사항 — 안전 고지 블록을 추가합니다**

- 원장 1인·직원 없음 구조에서는 **시술 후 이상 증상이 생겼을 때의 연락 경로를 페이지에 명시**해야 합니다.
  문헌상 중대 합병증(열화상·신경 손상)이 존재하고, 즉시 응대가 어려운 운영 구조이기 때문입니다.
- 진료시간 표 **바로 옆**에 `진료시간 외 이상 증상이 있을 때` 블록을 둡니다.
  연락이 닿지 않을 경우의 **대체 경로(가까운 응급실 등)** 를 반드시 함께 적습니다. → 6-⑩
- 이 블록은 마케팅 영역이 아니라 **안전 고지 영역**이므로 시각적으로 분리합니다.

- **캘린더는 1차 범위에서 제외합니다.** 참고 사이트(`15`)에는 월 캘린더가 있지만,
  우리는 일요일 외의 휴진일 데이터(공휴일·학회 등)를 관리할 방법이 정해지지 않았습니다.
  데이터 없이 캘린더를 만들면 "일요일만 회색인 달력"이 되어 표보다 정보량이 적습니다.
  휴진일 관리 방식이 정해지면 2차로 추가하겠습니다.

### ⑧ 오시는 길

- 주소(도로명 + 건물명 + 호수) / 도보 경로 / 지도 자리.
- ⚠️ **브리핑 내부 충돌이 있습니다.** §3은 `{{지도임베드}}`를 요구하는데
  §5는 "외부 요청은 Google Fonts만"이라고 못박았습니다. 지도 임베드는 외부 요청입니다.
  → **결정이 필요합니다** (아래 6-④).
- 도보 경로는 강남역 출구 번호 기준으로 서술 — 출구 번호와 소요 시간 모두 확인 필요.

### ⑨ 예약

- 완전예약제 고지를 **폼 바로 위에** (참고 사이트 `14` 구조 차용).
- 수집 항목은 **이름 · 연락처만**. 브리핑 범위를 넘지 않습니다.
- ⚠️ **구조적 문제 하나** — 이름·연락처만 받으면 희망 일시를 알 수 없어
  이 폼은 "예약 확정"이 아니라 **"예약 문의 접수"** 가 됩니다.
  버튼·안내 문구를 그에 맞게 쓰고, 확정이 아님을 사용자에게 명시하겠습니다.
  희망 일시 필드를 추가할지는 **결정이 필요합니다** (아래 6-⑤).
#### 개인정보 동의 — 조사에서 확인된 요건

**개인정보 보호법 제15조제2항의 4가지 고지 사항은 하나라도 빠지면 과태료 대상**입니다.
폼 안에서 바로 읽히게 배치합니다(별도 페이지로 숨기지 않음).

1. 개인정보의 **수집·이용 목적** — 진료 예약 문의 접수 및 회신
2. 수집하는 **개인정보의 항목** — 이름, 연락처
3. 개인정보의 **보유 및 이용 기간** — 수집일로부터 1년
4. **동의를 거부할 권리가 있다는 사실 및 거부에 따른 불이익** ← **법정 필수입니다. 빠뜨리면 안 됩니다**

**UI 제약 (법에서 나옵니다)**

| 항목 | 요건 |
|---|---|
| 사전 체크 | ⛔ **`checked` 기본값 금지** |
| 전체 동의 | ⛔ **`전체 동의` 하나만 두고 개별 동의를 생략하면 제22조제1항 위반.** 개별 체크박스를 둡니다 |
| 보유기간 | ⚠️ `1년`만 쓰지 말고 **기산점을 명시** — `수집일로부터 1년` |
| 주민등록번호 | ⛔ 수집 불가 |
| 전송 | **HTTPS 필수.** 규모와 무관하게 적용 |

> 참고: 조사에 따르면 예약용 이름·연락처는 제15조제1항제4호(계약 체결·이행)로
> **원칙적으로는 동의 없이도 수집 가능**합니다. 다만 브리핑이 동의 체크박스를 요구했고
> 그 편이 더 안전하므로 **동의 기반으로 갑니다.**

> ⚠️ **`시술 항목` `관심 부위` `증상`을 받는 순간 민감정보(건강정보)가 되어 별도 동의가 필요합니다.**
> 이름·연락처만 받는 한 민감정보가 아닙니다. 이것이 6-⑤ 결정의 핵심 기준입니다.

- 제출은 `submitReservation(payload)` **함수 하나로 분리**하고 내부는 `console.log`.
  나중에 `fetch` 한 줄로 교체 가능하게. 폼에는 `novalidate` 없이 네이티브 검증 + 커스텀 메시지.
- ⚠️ 나중에 **외부 폼·문자 발송 서비스를 쓰면 제26조 처리위탁**에 해당해
  수탁자명·위탁업무를 처리방침에 적어야 합니다. 서버 교체 시 함께 검토할 항목으로 남겨둡니다.
- 접근성: `label` 연결, 오류 메시지 `aria-live="polite"`, `autocomplete` 속성 지정.

### ⑩ 푸터

- 배경 `--ink`, 글자 `--paper` (명암비 15.58:1).
- 다단 사이트맵을 쓰지 않습니다(링크할 하위 페이지 없음).

#### 조사에서 확인된 사항 — 항목을 필수/선택으로 나눕니다

**홈페이지에 사업자등록번호·개설신고번호를 표기할 의무는 의료법에 없습니다.** 관행입니다.
(「전자상거래법」 제10조의 초기화면 표시 의무는 *통신판매업자* 대상이라 의원에 그대로 적용되지 않습니다.)

| 구분 | 항목 |
|---|---|
| **필수** | 의료기관명(슈리링의원) · 주소 · 대표전화 · 개인정보처리방침 링크 · **개인정보 보호책임자(원장 성명·연락처)** |
| **선택**(관행·신뢰도) | 대표자 · 사업자등록번호 · 개설신고번호 |

> ⚠️ 선택 항목을 표기한다면 **실제 신고서와 숫자를 대조**해야 합니다.
> 허위 표기는 제56조제2항제3호(거짓된 내용 표시)에 걸립니다.
> 브리핑 §3⑩이 요구했으므로 자리는 모두 만들되, 확인 전에는 플레이스홀더로 둡니다.

- ⚠️ 개인정보처리방침은 **링크만 있고 대상 페이지가 없으면 안 됩니다.**
  별도 페이지를 만들지, 같은 페이지 내 섹션으로 둘지 **결정이 필요합니다** (아래 6-⑥).

#### 안전 코어 블록 — 제57조제3항을 그대로 이용합니다

의료법 제57조제3항은 **다음만으로 구성된 광고는 심의를 받지 않아도 된다**고 규정합니다.

1. 의료기관의 **명칭 · 소재지 · 전화번호**
2. 의료기관이 설치·운영하는 **진료과목**
3. 소속 의료인의 **성명 · 성별 · 면허의 종류**

→ 이 네 가지만으로 된 **`기본정보` 블록**을 푸터에 두면,
**어떤 매체로 옮겨도 심의 없이 그대로 재사용할 수 있는 안전 코어**가 됩니다.
(나중에 네이버 플레이스나 명함·전단에 쓸 때 그대로 복사 가능)
성별까지 포함해야 한다는 점에 주의합니다.

---

## 4. 디자인 시스템

### 4-1. 색 — 실측 명암비와 용도 제한

브리핑 §4 토큰을 **전부 그대로** 유지합니다. 다만 각 색이 어디에 쓰일 수 있는지는
WCAG 기준으로 실측해 제한을 걸었습니다. (계산: sRGB 상대휘도, WCAG 2.1 정의)

| 토큰 | 값 | paper 위 | ink 위 | 허용 용도 |
|---|---|---|---|---|
| `--ink` | `#1A1714` | **15.58:1** | — | 본문·제목·헤더. 제한 없음 |
| `--paper` | `#F4EFE6` | — | **15.58:1** | 배경. ink 위 텍스트 |
| `--seal` | `#9E3B2F` | **5.88:1** ✅ | 2.65:1 ❌ | paper 위 포인트에만 소량. **ink 배경 위 사용 금지** |
| `--sand` | `#8D8272` | 3.29:1 ⚠️ | 4.73:1 ✅ | **본문 텍스트 불가.** 입력 테두리·아이콘 경계(3:1 통과)·ink 위 텍스트 |
| `--bronze` | `#A8895A` | 2.87:1 ❌ | 5.43:1 ✅ | paper 위 **텍스트 금지**. 장식 괘선·파문 그래픽. ink 위 텍스트는 가능 |
| `--bronze-hi` | `#C9A96A` | 1.96:1 ❌ | 7.96:1 ✅ | 동일. ink 배경 위 강조에 적합 |
| `--line` | `#D9D1C1` | 1.32:1 | 11.76:1 | **장식용 구분선 전용.** 입력 테두리로 쓰면 안 됨 |

#### ⚠️ 충돌 1 — `--sand`가 "보조 텍스트"인데 4.5:1을 넘지 못합니다

브리핑 §4는 `--sand`를 **보조 텍스트**로 지정했지만 paper 위 **3.29:1**로
§5의 "명암비 4.5:1 이상"을 만족하지 못합니다. 두 조항이 정면으로 충돌합니다.

**제안: 토큰을 변경하지 않고 하나를 추가합니다.**

```
--sand:      #8D8272   /* 유지 — 장식·입력 테두리·ink 위 텍스트 (비텍스트 3:1 통과) */
--sand-text: #736A5C   /* 추가 — paper 위 보조 텍스트 전용, 4.65:1 */
```

`#736A5C`는 `--sand`의 **색조(H)와 채도(S)를 그대로 두고 명도(L)만 낮춘** 값이라
색감이 달라지지 않습니다. 이렇게 하면 §4의 "변경 금지"를 지키면서 §5도 만족합니다.

#### ⚠️ 충돌 2 — 로고의 인장 빨강이 `--seal`과 다릅니다

로고(`01-logo.png`)에서 색을 추출한 결과:

| 요소 | 로고 실측 | 토큰 | |
|---|---|---|---|
| 글자 | `#1A1714` | `--ink #1A1714` | **완전 일치** |
| 배경 | `#F5F1E8` | `--paper #F4EFE6` | 사실상 동일 |
| 匠 인장 | **`#D6392C`** | `--seal #9E3B2F` | **불일치** (채널 최대 차이 56) |

로고의 빨강이 더 밝고 주황 쪽입니다. 게다가 `#D6392C`는 paper 위 **4.16:1**로
본문 텍스트 기준에 미달합니다(`--seal`은 5.88:1로 통과).

**제안: 토큰 `--seal #9E3B2F`를 그대로 쓰고, 로고는 이미지 그대로 둡니다.**
로고는 "찍힌 인장"이라는 독립된 물체로 읽히므로 페이지 포인트 색과 정확히 같을 필요가 없고,
`--seal`은 브리핑이 "포인트에만 아주 소량"으로 제한해 나란히 놓일 일이 거의 없습니다.
다만 **로고 색에 맞추길 원하시면 알려주세요** (아래 6-①).

#### 방울(鈴) — 파문 표현 방침

- **동심원만 씁니다.** 인라인 SVG, 원 3~4개, `stroke`는 `--line` 또는 `--bronze`, `fill` 없음, 낮은 불투명도.
- 배치는 **히어로 1곳 + 섹션 전환 1~2곳**으로 제한. 반복하면 패턴이 되어 정갈함이 깨집니다.
- **금지 — 무속·사찰 연상 요소:** 종·풍경 아이콘, 연꽃, 단청 배색, 태극, 부적 문양, 붓글씨 질감.
- 애니메이션: 히어로 진입 시 파문이 한 번만 아주 느리게 퍼짐(1.2s). `prefers-reduced-motion`에서 완전 정지.

### 4-2. 타이포그래피

참고 사이트 실측 위계(대제목 ≈ 본문 1.9× / 소제목 1.25× / 리드 1.07×)를 기준선으로 삼되,
한국어 · 375px 우선 · 본문 880px 제한에 맞게 재설계했습니다.

| 역할 | 375px | ≥880px | 폰트 | 비고 |
|---|---|---|---|---|
| 히어로 병원명 | 30px | 44px | Noto Serif KR 300 | 본문의 1.9× / 2.6× |
| 섹션 대제목 (h2) | 22px | 30px | Noto Serif KR 400 | |
| 소제목 (h3) | 18px | 20px | Noto Serif KR 400 | |
| 리드문 | 17px | 18px | Noto Sans KR 300 | |
| 본문 | **16px** | 17px | Noto Sans KR 400 | 기준값 |
| 보조·주석 | 14px | 14px | Noto Sans KR 400 | 색은 `--sand-text` |
| 영문 섹션 라벨 | 11px | 12px | Cormorant Garamond italic | 자간 0.18em |
| 숫자 악센트(가격·전화) | — | — | Cormorant Garamond | |

- **본문 `line-height: 1.72`** (참고 사이트 실측 1.69와 사실상 동일 — 검증됨)
- `word-break: keep-all` + `overflow-wrap: anywhere` 병행
  (keep-all만 쓰면 긴 URL·숫자에서 가로 넘침이 생깁니다)
- `letter-spacing: -0.01em` — 본문에만. **11~14px 작은 글자에는 적용하지 않습니다**(가독성 저하).
- Google Fonts: `preconnect` 2개 + `display=swap`, **필요한 weight만** 지정해 요청 크기 최소화.

### 4-3. 간격

8px 기반 스케일: `4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96 · 112`
섹션 상하 여백 64px(모바일) / 112px(데스크톱). 제목→본문 24px, 문단 사이 16px.

### 4-4. 포커스 표시

`:focus-visible` 에 `outline: 2px solid var(--ink); outline-offset: 2px;`
어두운 배경(푸터·하단 바) 위에서는 `--paper`로 반전. `--bronze`는 paper 위 2.87:1이라 **포커스 링에 쓰지 않습니다.**

---

## 5. 기술 설계

### 파일 구조

```
./
├── index.html
├── css/style.css
├── js/main.js
├── assets/          (비움 — 필요한 파일 목록은 README.md에)
├── reference/       (완료 — 스크린샷 16장 + 분석 문서)
├── CLAUDE.md        (다음 세션용 제약 요약)
├── PLAN.md          (이 문서)
└── README.md        (플레이스홀더 · 남은 작업 · 로컬 실행법)
```

### JavaScript 범위 — 최소로 유지

1. `submitReservation(payload)` — 예약 폼 제출. 현재 `console.log`, 나중에 `fetch`로 교체
2. 폼 검증 + 오류 메시지 `aria-live` 갱신
3. 앵커 스크롤 시 헤더 높이 보정 (CSS `scroll-margin-top`으로 처리 가능하면 JS 제외)
4. 모바일 하단 고정 바 — 예약 섹션 진입 시 숨김 (IntersectionObserver, 선택 사항)

캐러셀·아코디언·모달 없음. 프레임워크·빌드 도구 없음.

### SEO

- `<title>`, `<meta name="description">`, Open Graph, `canonical`, `theme-color`
- **JSON-LD `MedicalClinic`** — 이름 · 주소 · 전화 · 영업시간
  - `openingHoursSpecification`은 점심시간 때문에 **하루를 두 구간으로 분리**
    (`10:00–13:00`, `14:00–19:00`)해서 배열로 기술
  - 일요일은 항목에서 제외
  - ⚠️ 정확한 필드 표기(한국 주소를 `addressLocality`/`addressRegion`에 어떻게 나눌지,
    `medicalSpecialty` 값)는 **현재 schema.org 스펙 조사 중**입니다
- `og:image`는 **실제 이미지 파일이 필요합니다** (1200×630). 자산 목록에 포함

### 접근성 체크리스트 (구현 시 검수 항목)

- [ ] 명암비 — 위 4-1 표의 용도 제한 준수
- [ ] `:focus-visible` 전 인터랙티브 요소
- [ ] skip link
- [ ] landmark (`header`/`nav`/`main`/`footer`) + 각 `section`에 `aria-labelledby`
- [ ] 표에 `caption` + `th scope`
- [ ] 폼 `label` 연결 · 필수 표시 · 오류 `aria-live`
- [ ] `prefers-reduced-motion` (0s 대신 0.01ms — `transitionend` 미발생 방지)
- [ ] 375px에서 가로 스크롤 0

---

## 5-A. 카피 검수 체크리스트 — 구현 중 계속 돌립니다

### 의료법 제56조제2항 15개 호 대조

조사 결과, **슈링크 랜딩페이지의 기본 구성요소 상당수가 기본값으로 위반**입니다.
아래는 처음부터 **넣지 않는 것을 전제**로 설계합니다.

| 호 | 금지 내용 | 흔한 위반 요소 | 우리 처리 |
|---|---|---|---|
| 1 | 평가받지 않은 신의료기술 광고 | 자체 작명 시술명 | 설명형으로만 표기 |
| 2 | 치료경험담 등 치료 효과 오인 우려 | 후기·별점·비포애프터 | 전면 배제 |
| 3 | 거짓된 내용 | 허위 번호·미보유 장비명 | 확인 전 플레이스홀더 |
| 4 | 다른 의료인등과 **비교** | 타 장비 대비 비교표 | 배제 |
| 5 | 다른 의료인등 **비방** | — | 배제 |
| 6 | **시술 행위 직접 노출** | 시술 장면 사진·영상 | 배제 |
| 7 | **심각한 부작용 등 중요정보 누락** | 부작용 미기재·작은 글씨 | ④ 섹션 + 가격표 반복 |
| 8 | 객관적 사실 **과장** | `최고` `1위` | 배제 |
| 9 | 법적 근거 없는 자격·명칭 | `전문 클리닉` | 배제 |
| 10 | 기사형 광고 | `언론보도` 섹션 | 배제 |
| 13 | 할인·이벤트 등 | `특가` `한정` | 배제 |
| 14 | 비의료인 추천 | 연예인·인플루언서 | 배제 |

> ⚠️ 조사 권고: 홈페이지 하단에 근거를 적을 때 **"의료법 제56조"까지만 쓰고 항·호는 인용하지 않습니다.**
> 2018.9.28. 개정 전후로 항 번호가 달라 잘못 적을 위험이 있습니다.

### 금지 정규식 — 구현 후 HTML 전문에 돌립니다

```
없습니다 | 없어요 | 없는 | 무통 | 다운타임\s*(제로|0|없)
최고 | 최상 | 유일 | 국내\s*최초 | 1위 | 명의 | 전문\s*클리닉
특가 | 한정 | 이벤트 | 할인 | 선착순 | 혜택
즉시\s*효과 | 효과\s*보장 | 반영구 | 영구
```

`통증이 없습니다`는 **형사 유죄 사례가 있는 대표 위험 문구**입니다
(제7호 + 제8호 + 제2호 동시 접점). `없다`는 단정 대신
**개인차를 명시하는 서술**로 전환합니다. 단 모든 문장에 `개인차가 있습니다`를
기계적으로 붙이지 않고, 통증·기간을 언급한 문장 단위로 한 번씩만 배치합니다.

### ⚠️ 명칭 표기 규칙 — 로고와 헤더에 직접 영향

의료법 제42조 + 시행규칙 제40조: 의료기관 명칭은 **`고유명칭 + 종류명칭`** 구조이고,
**종류명칭의 글자 크기는 고유명칭의 1/2 이상**이어야 합니다.

- ✅ **`슈리링` + `의원`** 구조로 요건을 충족합니다.
- ✅ 제공된 로고도 통과합니다 — `슈리링의원` 네 글자가 한 줄에 같은 크기(17px)입니다.
- ⛔ **헤더·h1·로고에서 `의원`만 작게 줄이지 않습니다.** (흔한 디자인 처리인데 위반입니다)
- ⛔ `슈리링`만 단독으로 쓰지 않습니다. `슈리링클리닉` `슈리링 스킨` 같은 **변형 표기를 만들지 않습니다.**
  `<title>` · `og:title` · `h1` · JSON-LD `name` 전부 **`슈리링의원`** 전체 명칭을 씁니다.

### ⚠️ 전문과목 표기 — 가장 흔한 함정

**전문의가 아니면 전문과목을 표시할 수 없습니다**(의료법 제77조제2항 취지).
- 원장이 전문의가 아니라면 `피부과 전문의` `전문 클리닉` `전문의 직접 시술` 표현을 **전역에서 제거**하고
  `의사 {{원장성명}}`으로 표기합니다.
- 진료과목을 표시할 경우 `진료과목: 피부과` 형태로, **상호보다 작은 글자 크기**로 씁니다.
- → 원장 면허 종류 확인 필요 (6-⑫).

---

## 6. 결정해 주셔야 할 것

> **①~⑥은 구현 전에 답이 필요합니다.** ⑦은 구현과 병행 가능합니다.

### ① 로고 인장 색 — 토큰과 불일치

로고의 匠 인장은 `#D6392C`, 토큰 `--seal`은 `#9E3B2F`입니다.
- **(권장) 토큰 유지** — 로고는 이미지 그대로. `--seal`은 포인트에만 소량이라 충돌 거의 없음
- 로고 색에 맞춰 `--seal`을 `#D6392C`로 — 단 paper 위 4.16:1이라 **텍스트로는 쓸 수 없게 됩니다**
- 로고를 `--seal` 색으로 다시 제작

또한 **로고 원본 파일**이 필요합니다. 현재 것은 177×232 캡처(배경이 크림색으로 구워져 있고 하단에 57px 여백)라
헤더에 쓰기엔 해상도가 부족합니다. **SVG가 가장 좋고**, 없으면 배경 투명 PNG 2배수 이상.

### ② `--sand` 보조 텍스트 문제

`--sand`(3.29:1)가 §5의 4.5:1을 못 넘깁니다.
- **(권장)** `--sand`는 그대로 두고 `--sand-text: #736A5C`(4.65:1)를 **추가** — 색조·채도 동일, 명도만 하향
- `--sand`를 `#736A5C`로 교체
- 4.5:1 요건을 보조 텍스트에 한해 완화 (§5 수정)

### ③ 가격표에 부가세 포함 금액을 병기할까요?

현재는 `1,500원 / 1,500,000원 (부가가치세 별도)`입니다.
소비자가 실제 지불액을 바로 알 수 있게 **포함 금액(1,650원 / 1,650,000원)을 병기**하는 편이
고지 취지에 부합합니다.
- **(권장)** 별도 금액 + 포함 금액 병기
- 현행대로 "별도" 표기만

### ④ 지도 — 브리핑 §3과 §5가 충돌합니다

§3은 `{{지도임베드}}`를 요구하고 §5는 "외부 요청은 Google Fonts만"이라고 했습니다.
- **(권장) 임베드 없이** 주소 + 도보 경로 텍스트 + "지도 앱으로 열기" 링크 — 외부 요청 0, 쿠키 0
- 네이버/카카오 지도 iframe 임베드 — §5 위반, 제3자 쿠키 발생
- 지도 **이미지** 캡처 + 링크 — 외부 요청 0이지만 지도 저작권 확인 필요

### ⑤ 예약 폼에 희망 일시 필드를 넣을까요?

이름·연락처만으로는 30분 단위 예약을 특정할 수 없어 실질적으로 "문의 접수"가 됩니다.
- **(권장)** 브리핑대로 이름·연락처만. 문구를 "예약 문의"로 정확히 표기
- 희망 날짜·시간 필드 추가 — 조사 확인 결과 **희망 일시는 민감정보가 아니라 추가해도 안전**합니다.
  수집 항목 고지 문구만 함께 수정하면 됩니다.

> ⛔ 어느 쪽이든 **`시술 항목` `관심 부위` `증상` 필드는 넣지 않습니다.**
> 그 순간 민감정보(건강정보)가 되어 별도 동의 절차가 필요해집니다.

### ⑥ 개인정보처리방침 — 어디에 둘까요?

조사 확인: **1인 의원도 개인정보 처리방침 수립·공개 의무(제30조) 대상입니다. 규모 예외가 없습니다.**
또한 **"지속적으로 게재"** 해야 하며 **첫 화면 또는 첫 화면과 연결된 화면**에서 접근 가능해야 합니다.
→ 푸터 링크로 요건은 충족됩니다. 대상 페이지만 있으면 됩니다.

- **(권장)** `privacy.html` 별도 페이지 (파일 구조에 1개 추가)
- 같은 페이지 안의 섹션 + 앵커 링크
- 내용은 나중에 채우고 지금은 자리만

> 참고: **1인 의원은 개인정보 보호책임자를 별도 지정하지 않아도 되지만,
> 그 경우 원장(사업주)이 자동으로 보호책임자가 됩니다.** 푸터에 성명·연락처를 적게 됩니다.

### ⑦ 보유 기기를 특정해 주세요 — 가장 시급합니다

`슈링크`(Ultraformer III)와 `슈링크 유니버스`(Ultraformer MPT)는 **다른 제품**입니다.
페이지의 기기명·깊이 표기·허가 문구가 전부 여기에 종속됩니다.

| 필요한 것 | 어디서 |
|---|---|
| 실제 보유 모델명 | 장비 구매 서류 / 장비 본체 표기 |
| 보유 카트리지 mm 구성 | 장비 및 카트리지 재고 |
| **허가증의 `사용목적` 문구 전문** | 식약처 **의료기기안심책방**(emed.mfds.go.kr) `품목 및 업체검색` |
| 허가(인증) 번호 정확한 표기 | 위와 동일. 자료마다 `제허` / `제인` 표기가 엇갈려 **원본 대조 필수** |
| 첨부문서(사용설명서)의 `사용시 주의사항`·`금기` 원문 | 장비 첨부문서 |

> ⚠️ **허가번호는 확인 전에는 아예 쓰지 않는 편이 낫습니다.** 틀린 번호를 게재하는 것이
> 번호가 없는 것보다 위험합니다.

### ⑧ 샷(shot) 산정 기준

샷당 1,500원으로 과금하는데 **`샷`은 규제 용어가 아니라 업계 관용어**입니다.
점(dot) 방식과 선(line, MP) 방식에서 산정 기준이 달라질 수 있습니다.
- 페이지에 **샷의 정의를 명시**해야 분쟁을 피할 수 있습니다. 어떤 기준으로 세시는지 알려주세요.

### ⑨ 비급여 고지표 서식

HIRA 표준 웹페이지 서식 개발가이드가 있습니다. 열 구성을 이 표준에 맞출까요?
- **(권장)** 표준 서식 확인 후 열 구성을 맞춤 (구현 전 확인 필요)
- 지금 제안한 `항목 / 단가 / 비고` 3열로 진행

### ⑩ 시술 후 이상 증상 시 연락 경로

진료시간 외에 이상 증상이 생기면 어디로 연락해야 하나요?
1인 운영이라 즉시 응대가 어려울 수 있어 **대체 경로를 함께 적어야 합니다.**
- 별도 연락처(휴대전화 등)가 있나요?
- 대체 안내처로 적을 인근 병원·응급실이 있나요?

### ⑪ 예정된 외부 플랫폼 광고

네이버 플레이스·인스타그램 등에 같은 카피를 쓸 계획이 있나요?
자체 홈페이지는 **의료법 제57조의 사전심의 대상 매체가 아니지만**
(심의 대상은 일일 평균 이용자 10만 이상 매체 등 — 11평 의원 홈페이지는 해당 없음),
대형 플랫폼 광고는 대한의사협회 의료광고심의위원회 **사전심의 대상**입니다.
계획이 있다면 처음부터 심의 통과 가능한 수준으로 써 두면 재작성 비용이 없습니다.
(어느 쪽이든 **의료법 제56조는 매체와 무관하게 적용**되므로 현재 방침은 그대로입니다.)

### ⑫ 사실 확인이 필요한 운영 정보

브리핑에 없어 **지어낼 수 없는** 항목입니다.

| 항목 | 질문 |
|---|---|
| 토요일 진료 | "일요일 휴진"만 명시되어 있습니다. **토요일도 10:00–19:00 동일한가요?** 1인 운영이라 토요일 단축이 흔합니다 |
| 공휴일 | 휴진인가요? 진료시간표와 JSON-LD 양쪽에 반영됩니다 |
| 강남역 출구 | 도보 경로를 **몇 번 출구 기준**으로 쓸까요? |
| 원장 면허 | 푸터·JSON-LD에 표기할 진료과목이 **피부과 전문의**인지 **일반의**인지 (명칭 표시 규칙과 직결) |
| 시술 부위 | 1,000샷 구성의 대상 부위가 정해져 있나요? (얼굴 전체 / 부위 선택 등) |
| **마취크림과 30분 슬롯** | ⚠️ **예약 설계와 충돌할 수 있습니다.** 마취크림을 쓰신다면 대기시간이 필요한데, 대기 30분 + 시술 20분이면 **30분 슬롯 하나를 넘습니다.** 실제 운영에서 한 예약이 몇 슬롯을 쓰는지 확정해야 진료시간·예약 안내 문구를 정확히 쓸 수 있습니다 |

---

## 7. 플레이스홀더 — 어디서 구하는가

파일 최상단 `<!-- TODO -->` 주석에 모읍니다.

| 플레이스홀더 | 출처 |
|---|---|
| `{{대표전화}}` | 실제 개통 회선. 사업자등록증 또는 통신사 가입 정보 |
| `{{원장성명}}` | 의료기관 개설신고증 |
| `{{사업자등록번호}}` | 사업자등록증 (관할 세무서) |
| `{{개설신고번호}}` | **강남구보건소** 의료기관 개설신고필증 |
| `{{도메인}}` | 구매 예정 도메인. `canonical` · `og:url` · JSON-LD `url`에 사용 |
| `{{강남역도보시간}}` | 실측 필요. 출구 번호와 함께 확정 (위 6-⑦) |
| `{{지도임베드}}` | 위 6-④ 결정에 따라 형태가 달라짐 |
| `{{개원일}}` | 실제 개원 예정일 |

`assets/`에 필요한 파일 (README.md에 기재 예정):
- 로고 (SVG 권장 / 배경 투명 PNG 2배수)
- `og-image.png` 1200×630
- `favicon.svg` + `apple-touch-icon.png` 180×180

---

## 8. 승인 후 작업 순서

섹션 단위로 커밋합니다.

1. 뼈대 — `index.html` 골격 + `css/style.css` 토큰·리셋·타이포 + `CLAUDE.md`
2. 헤더 · 모바일 하단 고정 바
3. 히어로 + 방울 파문 SVG
4. 진료 안내
5. 진료 방식
6. 시술 전 확인사항 · 부작용
7. 비급여 진료비용 고지표
8. 진료시간 · 오시는 길
9. 예약 폼 + `js/main.js`
10. 푸터 + SEO/JSON-LD
11. 접근성·반응형 검수 → **375px / 1280px 실제 확인 후 문제 보고**
12. `README.md` 마무리

---

## 9. 이 계획에서 아직 확정하지 못한 것

정직하게 적습니다.

### ⚠️ 이 세션의 조사 환경 제약 — 반드시 알아두셔야 합니다

근거 조사 중 **`WebFetch`(원문 직접 열람)가 전 도메인에서 차단**되었습니다.
`law.go.kr`(국가법령정보센터), `mfds.go.kr`(식약처), `classys.co.kr`(제조사), PubMed 모두
이그레스 프록시에서 막혔습니다. `WebSearch`(검색)는 동작해서 **검색 결과 요약으로 교차 확인**했습니다.

**따라서 이 문서의 법령·허가사항 관련 내용은 2차 확인 수준입니다.**
조문 번호와 취지는 여러 출처에서 일치했지만 **원문을 1:1로 대조하지 못했습니다.**
실제 게재 전에 반드시:
- 법령은 국가법령정보센터에서 최신 개정본 확인
- 허가사항은 원장이 **보유 장비의 허가증·첨부문서 원문**에서 직접 확인
- 부가세 과세 여부는 **세무사 확인**

이 문서의 문구를 그대로 복사해 게재하지 마십시오.

### 확정된 것 / 확정되지 않은 것

| 내용 | 확인 수준 |
|---|---|
| HIFU 기전(열응고점 형성, 60~70℃) | **높음** — 학술 문헌(J Cosmet Dermatol 2025) |
| 깊이–조직층 대응(1.5 / 3.0 / 4.5mm) | **높음** — 학술 문헌 |
| 흔한 이상반응(홍반·부종·불편감·저릿함·압통) | **높음** — 합병증 리뷰(Appl. Sci. 2025) |
| 중대 이상반응(열화상·신경손상·지방위축·과색소침착) | **높음** — 동일 문헌 |
| 의료법 제45조·시행규칙 제42조의2 비급여 홈페이지 고지 의무 | 중간 — 검색 교차확인, 원문 미대조 |
| 의료법 제57조 사전심의 대상 매체 기준 | 중간 — 동일 |
| 의료기기법 제24조 허가사항 외 표시 금지 | 중간 — 동일 |
| 슈링크 / 슈링크 유니버스 제품 구분 | 중간 — 제조사 보도자료 |
| 허가 사용목적 문구, 허가번호 | **낮음** — 매체 인용만. **원본 대조 필수** |
| 시술 과정 순서·소요시간 | **낮음** — 1차 출처 없음. 원장 실제 운영 기준으로 작성 |
| 금기 목록 | **낮음** — 임상 웹페이지 기반. 첨부문서 원문 필요 |
| 멍·근육통 유사 통증 | **문헌 확인 실패** |
| 1,000샷 부위별 배분 | **표준 없음** — 게재하지 않기로 결정 |

### 그 밖에

- ③·⑤ 섹션의 최종 문안은 **반드시 원장 감수**를 받아야 합니다.
  제가 쓰는 것은 초안이며, 의학적 사실의 최종 책임은 제가 확인할 수 없습니다.
- **모바일 CTA 배치**는 참고 자료에 모바일 캡처가 없어 근거 없이 새로 설계합니다.
- 개인정보 동의 문구 요건과 schema.org `MedicalClinic` 필드 표기는 **조사가 아직 진행 중**입니다.
  결과가 나오면 반영하겠습니다. 구현을 막는 항목은 아닙니다.
